<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:template="clr-namespace:XpertMobileApp.Views.Templates"
             x:Class="XpertMobileApp.Views.RfidScanPage"
             xmlns:i18n="clr-namespace:XpertMobileApp.Helpers"
             xmlns:scroll="clr-namespace:Xamarin.Forms.Extended;assembly=Xamarin.Forms.Extended.InfiniteScrolling"
             Title="{Binding Title}"
             x:Name="Tiers">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="btn_Filter"  Text="{i18n:Translate btn_Filter_RFID_Text}" Clicked="Filter_Clicked">
            <ToolbarItem.Icon>
                <OnPlatform x:TypeArguments="FileImageSource">
                    <On Platform="UWP" Value="filter.png"/>
                </OnPlatform>
            </ToolbarItem.Icon>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <StackLayout VerticalOptions="FillAndExpand">
        <!-- Filter -->
        <StackLayout x:Name="FilterPanel" IsVisible="False" Padding="0" Margin="0" Spacing="0" Orientation="Vertical" VerticalOptions="Start"
                         HorizontalOptions="Fill" BackgroundColor="{StaticResource Accent}">

            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>


                <StackLayout Orientation="Horizontal" Grid.Column="0" Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label   Text="{i18n:Translate lbl_Continues_scan_RFID}" Margin="5, 0, 0, 0" />
                    <Switch x:Name="CScan" IsToggled="{Binding ContinuesScan, Mode=TwoWay}"></Switch>
                </StackLayout>


                <StackLayout Orientation="Horizontal" Grid.Column="1" Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label   Text="Anti" Margin="5, 0, 0, 0" />
                    <Switch x:Name="AntiP" IsToggled="{Binding Anti, Mode=TwoWay}"></Switch>
                </StackLayout>
                <StackLayout Orientation="Horizontal" Grid.Column="0" Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Q (1 - 15):"  Margin="5, 0, 0, 0" />
                    <Entry x:Name="qvalue" Text="{Binding q, Mode=TwoWay}"/>
                </StackLayout>
                <!-- Button -->
                <Label Grid.Column="1" Grid.Row="1" Text="{Binding ElementsCount, StringFormat='Total Tags. {0}',Mode=TwoWay}" Margin="5, 0, 0, 0" />
                <StackLayout Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Padding="0" Margin="0,0,0,0" Spacing="0" Orientation="Horizontal" VerticalOptions="End"
                         HorizontalOptions="Fill">

                    <Button x:Name="btn_Clear" Margin="5" HorizontalOptions="FillAndExpand" Text="{i18n:Translate btn_Clear_Text}" Style="{DynamicResource DefaultBtnStyle}" 
                        Clicked="btn_Clear_Clicked"/>
                    <Button x:Name="btn_Scan" Margin="5" HorizontalOptions="FillAndExpand" Text="{i18n:Translate btn_scan_Rfid_Text}" Style="{DynamicResource DefaultBtnStyle}" 
                        Clicked="btn_Scan_Clicked"/>

                    
                </StackLayout>


            </Grid>
            <StackLayout Orientation="Horizontal" Padding="5">
            <Button x:Name="btn_Scan_CB" Margin="5" Text="{i18n:Translate btn_scanCodeBarre}" Style="{DynamicResource DefaultBtnStyle}"
                        Clicked="btn_Scan_CB_Clicked"/>
                <Entry x:Name="codebrarelot"  Text="{Binding CODE_BARRE_LOT, Mode=TwoWay}" HorizontalOptions="FillAndExpand"></Entry>
            </StackLayout>
        </StackLayout>
        <ScrollView Orientation="Vertical" VerticalOptions="Fill" >
      
            <StackLayout VerticalOptions="FillAndExpand">

            <Frame CornerRadius="10" Padding="5" Margin="5" BackgroundColor="White" HasShadow="True"  OutlineColor="White">
                <Grid  BackgroundColor="White" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackLayout Grid.Column="0" Grid.ColumnSpan="2" Spacing="0" Padding="0,5">
                        <Label Text="{i18n:Translate txt_header}" TextColor="{StaticResource Primary}" FontSize="Large" FontAttributes="Bold"/>
                        <BoxView BackgroundColor="#EFEFEF" HeightRequest="0.5"></BoxView>
                    </StackLayout>

                    <StackLayout Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Spacing="5" Padding="0">
                        <Label  Text="{Binding CurrentLot.DESIGNATION_PRODUIT, Mode=TwoWay}" Margin="5, 0, 0, 0" />
                    </StackLayout >
                    <StackLayout Orientation="Horizontal" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Spacing="5" Padding="0">
                        <Label  Text="{i18n:Translate lbl_lot_txt} " Margin="5, 0, 0, 0" />
                        <Label  Text="{Binding CurrentLot.LOT, Mode=TwoWay}" Margin="5, 0, 0, 0" />
                        <Label  Text="{Binding CurrentLot.PPA, StringFormat='PPA. {0:N2}',Mode=TwoWay}" Margin="5, 0, 0, 0" />
                        <Label  Text="{Binding CurrentLot.SHP, StringFormat='SHP. {0:N2}',Mode=TwoWay}" Margin="5, 0, 0, 0" />
                    </StackLayout>
                </Grid>
            </Frame>
        <Frame CornerRadius="10" Padding="5" Margin="5" BackgroundColor="White" HasShadow="True"  OutlineColor="White">
            <StackLayout x:Name="sl_rows" VerticalOptions="Start" >

                <StackLayout Grid.Column="0" Grid.ColumnSpan="2" Spacing="0" Padding="0,5">
                    <Label Text="{i18n:Translate txt_Details}" TextColor="{StaticResource Primary}" FontSize="Large" FontAttributes="Bold"/>
                    <BoxView BackgroundColor="#EFEFEF" HeightRequest="0.5"></BoxView>
                </StackLayout>

                <ListView x:Name="ItemsListView"
                    ItemsSource="{Binding Items}"
                    VerticalOptions="FillAndExpand"
                    HasUnevenRows="true"
                    IsPullToRefreshEnabled="false"
                    IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                    CachingStrategy="RecycleElement"
                    ItemSelected="OnItemSelected">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <template:RfidTemplate />
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </Frame>
        </StackLayout>
        </ScrollView> 
        <StackLayout>
                <Button x:Name="SaveRfids" Text="{i18n:Translate sp_btn_SaveRfid}" Clicked="SaveRfids_Clicked" Style="{DynamicResource DefaultBtnStyle}"/>
        </StackLayout>
    </StackLayout>

</ContentPage>