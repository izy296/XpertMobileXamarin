<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                          xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
             xmlns:i18n="clr-namespace:XpertMobileApp.Helpers"
             xmlns:flex="clr-namespace:Flex.Controls;assembly=Flex"
             x:Class="XpertMobileApp.Views.NewTiersPopupPage"
                 >
    <ContentPage.Content>

        <StackLayout VerticalOptions="Center" HorizontalOptions="FillAndExpand" Padding="10">
            <Frame CornerRadius="15" Padding="10">
              <StackLayout VerticalOptions="FillAndExpand" BackgroundColor="White" Padding="10">

                <Label Text="{i18n:Translate txt_Name}" Margin="5, 0, 0, 0" FontAttributes="Bold"/>
                <Entry Placeholder=""
                       Margin="30, 0, 30, 30"
                       Text="{Binding Item.NOM_TIERS, Mode=TwoWay}"
                       TextColor="{StaticResource Primary}"/>

                <Label Text="{i18n:Translate txt_LastName}" Margin="5, 0, 0, 0" FontAttributes="Bold"/>
                <Entry Placeholder=""
                       Margin="30, 0, 30, 30"
                       Text="{Binding Item.PRENOM_TIERS, Mode=TwoWay}"
                       TextColor="{StaticResource Primary}"/>

                <Label Text="{i18n:Translate txt_phone}" Margin="5, 0, 0, 0" FontAttributes="Bold"/>
                <Entry Placeholder=""
                       Margin="30, 0, 30, 30"
                       Text="{Binding Item.TEL1_TIERS, Mode=TwoWay}"
                       TextColor="{StaticResource Primary}"/>
                  
                    <Label Text="{i18n:Translate tct_Type}" Margin="5, 0, 0, 0"  FontAttributes="Bold"/>
                <Picker ItemsSource="{Binding Types}"
                                ItemDisplayBinding="{Binding DESIGNATION_TYPE}"
                                SelectedItem="{Binding SelectedType}"
                                TextColor="{StaticResource Primary}"
                                SelectedIndexChanged="TypesPicker_SelectedIndexChanged"
                                Margin="30, 0, 30, 30" 
                                x:Name="TypesPicker"/>

                <Label Text="{i18n:Translate txt_Categorie}" Margin="5, 0, 0, 0" FontAttributes="Bold"/>
                <Picker ItemsSource="{Binding Familles}"
                         ItemDisplayBinding="{Binding DESIGN_FAMILLE}"
                         SelectedItem="{Binding SelectedFamille}"
                                TextColor="{StaticResource Primary}"
                                SelectedIndexChanged="FamillesPicker_SelectedIndexChanged"
                                Margin="30, 0, 30, 30" 
                                x:Name="FamillesPicker"/>


            </StackLayout>
            </Frame>
            <StackLayout Padding="0" Margin="0" Spacing="0" Orientation="Horizontal" VerticalOptions="EndAndExpand">

                <flex:FlexButton
                    x:Name="cmd_Terminate"
                    HorizontalOptions="FillAndExpand"
                    IconOrientation="Left" 
                    IconPadding="0,0,5,0"
                    MinimumHeightRequest="50"
                    HeightRequest="55"
                    Padding="0"
                    Margin="0"
                    Text="Annulé"
                    CornerRadius="38"
                    ForegroundColor="#ffffff"
                    HighlightForegroundColor="#49516F"
                    BackgroundColor="#6279B8"
                    HighlightBackgroundColor="#8EA4D2"
                    BorderColor="#8EA4D2"
                    HighlightBorderColor="#8EA4D2"
                    BorderThickness="3"
                    Clicked="btn_Cancel_Clicked"
                    />

                <flex:FlexButton
                    x:Name="cmd_Buy"
                    HorizontalOptions="FillAndExpand"
                    IconOrientation="Left" 
                    WidthRequest="120"
                    IconPadding="0,0,5,0"
                    HeightRequest="55"
                    Padding="0"
                    Margin="0"
                    Text="Valider"
                    CornerRadius="38"
                    ForegroundColor="#ffffff"
                    HighlightForegroundColor="#496f4f"
                    BackgroundColor="#62b869"
                    HighlightBackgroundColor="#8ed293"
                    BorderColor="#8ed293"
                    HighlightBorderColor="#8ed293"
                    BorderThickness="3"

                    Clicked="btn_Save_Clicked"
                    />

            </StackLayout>
        </StackLayout>
        
    </ContentPage.Content>
</pages:PopupPage>