<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:gv="clr-namespace:GoogleVisionBarCodeScanner;assembly=BarcodeScanner.XF"
             x:Class="XpertMobileApp.Views.GoogleVisionBS"
             xmlns:fontawesome="clr-namespace:FontAwesome"
             Title="Scannez votre produit">
    <ContentPage.ToolbarItems>

        <ToolbarItem ClassId="Scan" Clicked="RowScan_Clicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource FontFamily="{StaticResource FontAwesomeSolid}" Size="Subtitle" 
                Glyph="{x:Static fontawesome:FontAwesomeIcons.Barcode}" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>

    <!--
        <ToolbarItem Text="Impr" Icon="Selection_32.png" Clicked="AchImpr_Clicked" />
        <ToolbarItem Text="{i18n:Translate lp_btn_Settings}"  Icon="settings_32.png" Clicked="HeaderSettings_Clicked"  />
        -->

    </ContentPage.ToolbarItems>


    <ContentPage.Content>
        <StackLayout>
            <gv:CameraView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" 
                           OnDetected="CameraView_OnDetected" DefaultTorchOn="False" VibrationOnDetected="False" AutoStartScanning="True" RequestedFPS="30" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>