<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XpertMobileApp.Views.Templates.RfidTemplate">
    <ContentView.Content>

        <StackLayout>
            <Frame CornerRadius="3" Padding="2" Margin="2" BackgroundColor="Transparent" HasShadow="True"  OutlineColor="Transparent">
               
                    <StackLayout Orientation="Horizontal" Spacing="5" Padding="0">
                        <Label Text="{Binding EPC}" TextColor="{StaticResource DefaultTextColor}"/>
                        <Label Text="{Binding COUNT ,Mode=TwoWay}"  TextColor="{StaticResource Accent}"/>
                        <Label Text="{Binding RSSI ,Mode=TwoWay}"  TextColor="{StaticResource Accent}"/>
                    </StackLayout>

            </Frame>
        </StackLayout>
  </ContentView.Content>
</ContentView>